use pjsh_core::Context;

/// Completes a word based on a prefix.
pub fn complete(
    _prefix: &str,
    _words: &[&str],
    _word_index: usize,
    _context: &Context,
) -> Vec<String> {
    vec![]
}
