[package]
name = "pjsh_core"
version = "0.1.0"
license = "MIT"
edition = "2021"

[dependencies]
parking_lot = {version = "0.12.1", features = ["deadlock_detection"] }

pjsh_ast = { path = "../pjsh_ast" }
