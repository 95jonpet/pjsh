#!/bin/pjsh

list := [
  literal
  ${HOME}
  "quoted word"
]

echo `List length: ${list | len}`
echo `Element #0: ${list | nth 0}`
echo `Element #1: ${list | nth 1}`
echo `Element #2: ${list | nth 2}`
