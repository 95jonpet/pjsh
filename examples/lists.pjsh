#!/bin/pjsh

list := [
  literal
  ${HOME}
  "quoted word"
]

echo `List length: ${list | len}`
echo `Element #0: ${list | index 0}`
echo `Element #1: ${list | index 1}`
echo `Element #2: ${list | index 2}`
